{
  "name": "treacon",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "test": "vitest run",
    "test:watch": "vitest",
    "zip": "cd dist && zip -r -X ../treacon-$npm_package_version.zip ."
  },
  "devDependencies": {
    "@crxjs/vite-plugin": "^2.6.0",
    "typescript": "^5.5.0",
    "vite": "^7.0.0",
    "vitest": "^4.0.0"
  }
}
